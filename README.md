# hans-wehr-fodt-parser
A tool for parsing the Hans Wehr dictionary from fodt format to plain-old-xml

To setup:
 - Run bundle to install all dependancies
 - build the xml file by running ``` ruby parser.rb ```
